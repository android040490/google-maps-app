import React from 'react';

import MapContainer from 'containers/MapContainer';

const MainPage = () => {
    return (
        <div>
            <MapContainer/>
        </div>
    );
};

export default MainPage;