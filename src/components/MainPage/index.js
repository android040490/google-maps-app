import React from 'react';

import MapContainer from 'components/MapContainer';

const MainPage = () => {
    return (
        <div>
            <MapContainer/>
        </div>
    );
};

export default MainPage;